version: "2"
services:
  schedule_service:
    container_name: schedule_service
    image: schedule_service
    volumes:
      - /root/schedule_service/logs:/app/logs
    restart: always
    network_mode: host